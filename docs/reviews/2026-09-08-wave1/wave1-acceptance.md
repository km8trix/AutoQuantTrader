# Wave 1 acceptance — 2026-09-09

The bounded data, standard runtime and authorized account-read feasibility gates in implementation-plan section 5 are met. GitHub commit, required checks, PR merge and merged-revision verification remain closeout steps. This acceptance permits the next offline engine wave after closeout; it grants no connected execution admission.

- A imported/reloaded 20 real Tiingo bars across five sessions, with explicit current-vintage and modeled historical-availability limits.
- C proved halted startup, duplicate exclusion, normal SIGTERM stop and halted restart from the installed standard wheel.
- B demonstrated owner OAuth, supervised renewal, explicit dedicated-account selection, fresh account discovery and a complete bounded production balance/portfolio/current-order/order-history/activity traversal under `personal-v1/account-eligibility/2`. Account identity matched. CASH discovery and MARGIN balance observations are both preserved; the explicit profile permits both modes. The production read no longer stops on that difference.

Initial empty HTTP 204 portfolio/order/activity responses are retained as query observations. They do not establish zero holdings/orders, usable execution/correction identities, reconciliation or releasable commitments. The shared Order API table lists 204 data-not-found, while List Orders' response table omits it; this ambiguity remains explicit. No error code was invented for an empty body. Sandbox balance identity still fails; the production result does not qualify sandbox or relabel stored examples as current economics.

The frozen account/runtime contract says W1 freezes actual endpoint/account quotas. Actual quotas remain unavailable: the implementation freezes only a local ceiling of ten reads per rolling minute, one attempt per read and a three-second deadline. This is an unresolved provider-feasibility obligation that blocks connected admission, not a claim that provider quotas passed. The same explicit connected/execution blocks apply to USD, cash-field semantics, margin liability/calls/restrictions, the mode disagreement, quote rights/freshness, retention and usable recovery identities. Applied reconciliation belongs to W4 and forward protocol/readiness closure to W6; a single W1 observation never satisfies either.

This interpretation follows the plan's concrete authorized session/account-read exit and the amendment's distinction between permitted read traversal and execution qualification. It does not waive an execution requirement or edit the original W0 contract bytes. Root and an independent runtime reviewer checked the gate mapping; root and an independent data reviewer checked the margin profile and initial-204 handling. See [capture metadata](live-broker-production-cash-funded.json), [verification](account-eligibility-verification.json) and [integrated results](README.md).
