# Wave 1 integrated evidence — 2026-09-08

**Wave 1 A data, B authorized production account-read feasibility and C runtime passed their bounded gates. GitHub closeout is pending.** Connected financing, quotes, quotas and reconciliation remain unqualified; sandbox identity still fails. The [acceptance rationale](wave1-acceptance.md) explains this distinction. The [canonical plan](../../IMPLEMENTATION_PLAN.md) remains the only roadmap.

Base: `107fa791bb52e9fa42cbce65992ea1ce9168834e`, branch `codex/personal-use-planning-20260908`, with preserved uncommitted W0 documents and new uncommitted W1 changes. Three isolated workers implemented data, broker and runtime; root reviewed their exact allowlists, shared schema, composition, build/checker/CI changes and integrated sequentially. [Implementation hashes](implementation-manifest.json) identify the accepted file contents. No commit, push, PR or merge was performed.

| Gate | Result | Evidence and limits |
|---|---|---|
| A real data | Passed | Four Tiingo GETs, five sessions per DIA/IWM/QQQ/SPY (Feb 3–7 2025), 20 real bars. Private immutable archive reloaded byte-identically and replayed at six availability frontiers. [Verification](real-data-verification.json), [capture metadata](tiingo-capture-metadata.json) |
| C standard process | Passed | Installed wheel outside the source checkout, without distribution dependencies: HALTED startup, duplicate exclusion, SIGTERM stop in 0.024 s, halted restart, unknown residual broker exposure. [Verification](installed-runtime-verification.json), [events](check-output/runtime-events.json) |
| B actual account read | Traversal complete; financing/recovery still unqualified | OAuth and renewal succeeded. Owner-selected margin privileges are accepted under the cash-funded policy. Production completed the bounded read traversal; terminal operations: activity, balances, orders_current, orders_history, portfolio. [Latest capture](live-broker-production-cash-funded.json), [amendment/checks](account-eligibility-verification.json). Sandbox identity and provider qualification limitations remain explicit. |
| Python regression | Passed with explicit skips | **1,264 passed, 7 PostgreSQL skipped**, one existing FastAPI/Starlette deprecation warning. Includes new foundations and retained financial, reservation, control, temporal, OAuth, Tiingo and SQLite/API tests. [Output](check-output/personal-suite.txt) |
| Quality/contracts | Passed | Ruff formatting 715 files; Ruff lint; mypy 291 source files; generated API contracts; standard architecture boundaries; Compose model. [Commands/results](commands.json) |
| Packaging | Passed | Two ordinary `py3-none-any` wheel builds were byte-identical with no hook environment override, using pinned cached build dependencies. Installed start/stop proof used a clean environment. [Build evidence](wheel-verification.json) |

The real sample is **exploratory validated current-vintage history** under the owner's explicit existing-Tiingo personal-use authorization and local source review. It is not independently qualified provider/PIT data. The actual capture is dated 2026-09-09 UTC; historical session dates are 2025, vendor publication is unknown, and historical availability is explicitly modeled at 20:00 America/New_York. At the first session's open, close and just before modeled availability, replay returns zero bars. At the first availability boundary it returns four; the final frontier returns all twenty. Raw and adjusted prices are distinct, with zero splits/dividend events in this sample. Fixture tests cover dividend exclusions and split rejection. This five-session test is not the full research study period; the selected 2025 dates have now been accessed for engineering validation and must not be described as untouched holdout.

The sample calendar is a bounded review of the official [NYSE 2025 calendar](https://www.nyse.com/publicdocs/ICE_NYSE_2025_Yearly_Trading_Calendar.pdf) and [session hours](https://www.nyse.com/markets/hours-calendars). Its manifest pins the supplied session grid and local timezone-file digest. [Tiingo's documentation](https://www.tiingo.com/documentation/end-of-day) supports raw/adjusted and action-field interpretation; payable timing, historical publication and execution quotes are not invented. Raw licensed payloads and the 13,560-byte archive remain outside Git at `/Users/spencer.karrat/Documents/AutoQuantTrader/personal-data/tiingo-wave1-20260908`. Only metadata/hashes are retained here. The capture used the frozen worker helper; root subsequently added a tested rejection of destinations inside the checkout without changing its HTTP/receipt behavior.

The broker lane includes 91 account-read tests, 33 owner OAuth tests and 29 supervised renewal tests. Independent review covered account/environment binding, credential rotation, policy-version pinning, redaction and the initial no-content response handling. Owner consent, explicit account selection and dedicated-use declaration are complete. OAuth and supervised renewals succeeded in both environments.

The [final production capture](live-broker-production-cash-funded.json) completed discovery, balances, portfolio, current orders, bounded order history and activity under the [account eligibility amendment](../../contracts/personal-v1/account-eligibility-amendment.md). Discovery CASH and balance MARGIN remain distinct; the owner-authorized profile permits both. Portfolio/order/activity no-content responses remain unreconciled observations. All funding/reconciliation/order authority flags remain false. Earlier stopped captures and the sandbox balance identity failure remain in their metadata records. The [sandbox refresh](live-broker-sandbox-refresh.json) preserves aggregate renewal/discovery metadata; per-account listings and private paths were removed from this public copy. [Publication provenance](publication-redactions.json) records the redactions without private values. Across all recorded attempts there were 22 account GETs and six OAuth token requests. See the [read-only runbook](../../runbooks/personal-v1-etrade-readonly.md).

Standard architecture checks retain dependency direction and pure financial/strategy import boundaries and verify the complete simulation import closure, including package initializers. Mutation tests exercise these checks. They are static regression guards, not a Python security sandbox. The ordinary package disables the historical native build hook by default and has no native payload. Default Make/CI checks use the explicit standard suite. Native sources, old manifests, remote-anchor code and exact-seal tooling remain preserved; the old workflow is manual-only, still subject to its historical prerequisites and known seal/prose drift. No stale seal was regenerated to manufacture a pass.

Physical suspend/wake, an external host-time offset producer, sandbox account capture, connected economic qualification, PostgreSQL locking, remote CI and container/native activation were not qualified locally. No financial schema/migration or browser code changed. W0's 100 browser/bundle passes remain historical evidence; they were not rerun for W1. The old `.venv` launcher remains preserved; the [foundation runbook](../../runbooks/personal-v1-foundations.md) uses a separate environment. All original worktree HEADs and statuses are checked in [final preservation](final-preservation.json); the frozen W0 contract/evidence bytes remain unchanged, with the new dated account eligibility amendment governing current consumers, while pre-W1 versions of the intentionally updated documents are retained under `prior-documents/`.

The remaining closeout is commit, GitHub PR, required checks, merge and verification of the merged revision. Then this orchestration task starts W2. See [acceptance](wave1-acceptance.md) for retained connected/execution blockers; no order, native lifecycle, deployment or financial authority is granted.
