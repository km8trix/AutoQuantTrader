# Development proxy used by Vite. Browser code always calls same-origin /api/v1 URLs.
VITE_API_PROXY_TARGET=http://localhost:8000

# Explicitly opt in to deterministic UI fixtures when the development API is unavailable.
# This flag is ignored by production builds.
VITE_USE_DEV_FIXTURES=false
